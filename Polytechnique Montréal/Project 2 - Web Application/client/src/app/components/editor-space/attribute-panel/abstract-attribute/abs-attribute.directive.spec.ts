import { AbsAttributeDirective } from './abs-attribute.directive';

describe('AbsAttributeDirective', () => {
    it('should create an instance', () => {
        const directive = new AbsAttributeDirective();
        expect(directive).toBeTruthy();
    });
});
